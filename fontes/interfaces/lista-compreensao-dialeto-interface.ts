import { ConstrutoInterface } from "@designliquido/delegua/interfaces";

export interface ListaCompreensaoDialeto {
    lista?: ConstrutoInterface;
    iteravel?: ConstrutoInterface;
    expressao?: ConstrutoInterface;
}
